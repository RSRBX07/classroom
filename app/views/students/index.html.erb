<h1>Liste des étudiants</h1>
<table>
  <tr>
    <th>Nom</th>
  </tr>
  <% @students.each do |student|  %>
    <tr>
      <td><%= link_to student.name, controller: "students", action: "show", id:student %></td>
    </tr>
  <% end %>
</table>
<h2><%= link_to "voir la liste des cours", courses_index_path %></h1>